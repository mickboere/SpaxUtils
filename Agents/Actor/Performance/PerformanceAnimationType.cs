namespace SpaxUtils
{
	public enum PerformanceAnimationType
	{
		Animator = 0,
		Poser = 1,
		/// <summary>Single clip with a performer-owned playhead; timing comes from the timeline's markers.</summary>
		Timeline = 2
	}
}
